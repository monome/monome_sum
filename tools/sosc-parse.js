inlets = 1
outlets = 1

var stuff = new Array(3);

/*function sosc(stuff) {
	for (i=0; i<stuff.length; i++) post(stuff[i]);
	post();

	outlet(0,stuff);
}
*/
function sys_size(stuff[]) {
	outlet(0,stuff);
}